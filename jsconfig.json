{
  "// メモ": [
    "overlay は Vanilla JS で書く(docs/decisions/ の「overlay の実装」)。",
    "型は .ts へ移行せず JSDoc で書き、tsc は検査だけに使う(noEmit)。",
    "実行されるのは素の JS のままで、ビルド成果物は増えない。",
    "",
    "検査対象は src/overlay/ と src/zoom/comment-source.js。",
    "後者は Zoom payload を OverlayComment へ正規化する部分で、SDK に依存せず",
    "素のオブジェクトだけを扱うので検査できる。",
    "",
    "src/zoom/main.js と src/server/ は外している。Zoom SDK / Express / Node API に",
    "依存していて、型を通すには @types/node の追加と既存コードの書き換えが要る。",
    "コメントの正規化とは別の作業なので広げない。"
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM"],
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "types": []
  },
  "include": ["src/overlay/**/*.js", "src/zoom/comment-source.js"]
}
